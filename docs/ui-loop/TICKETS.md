# FuelWell UI Sprint — Tickets (2026-06-12)

Deduped from per-route audit findings. 12 tickets, ranked by user impact / effort.
Constraint per ticket: < ~150 changed lines, one executor, one worktree, no strategy/brand/nav/scope changes.
"Smoke 21/21" = `pnpm exec playwright test` (tests/smoke.spec.ts + tests/coach.spec.ts, 21 tests total).

| Rank | ID | Route | Title | Severity | Files | Verification |
|---|---|---|---|---|---|---|
| 1 | FW-01 | /app/recovery | Fix broken next-action hrefs, spec-language copy, and checklist heading contrast | high | src/app/app/recovery/page.tsx | Clicking each of the 3 Next-action cards lands on /app/log, /app/workouts, /app/activity (200, not 404); page contains neither "examples above" nor "until integrations exist"; "RECOVERY CHECKLIST" h2 is text-neutral-500; smoke stays 21/21 |
| 2 | FW-02 | /app/coach | Fix mobile scroll-container bug (min-h-dvh) plus header contrast/tap and chip height | high | src/app/app/coach/page.tsx | Screenshot at 375px shows the Coach header with "New chat" visible without scrolling and the welcome card unclipped; "New chat" + subtitle are text-neutral-600; quick-prompt chips measure >=44px tall in devtools; coach.spec.ts stays 12/12 |
| 3 | FW-03 | /app/log | Surface the "Logging for" meal selector above search on mobile; add aria-pressed to mode and meal buttons | high | src/app/app/log/page.tsx | Screenshot at 375px shows the Breakfast/Lunch/Dinner/Snack card above FoodSearch results; desktop screenshot unchanged (selector still right column, no duplicate visible); accesslint audit_live on /app/log reports aria-pressed on all 7 toggle buttons; smoke 21/21 |
| 4 | FW-04 | /app/grocery-list | Touch targets (check/trash/chips), empty state, and state-aware summary copy | high | src/app/app/grocery-list/page.tsx | At 375px the check toggle and trash buttons measure >=44px hit area in devtools (visual size unchanged); deleting all items renders the EmptyState ("Nothing to shop"); after "Mark all shopped" the summary reads "All shopped for this week." with no "4 planned days" text; smoke 21/21 |
| 5 | FW-05 | /app/log | Logged-meals: Remove/Edit touch targets >=44px and neutral-400 text to neutral-500 | medium | src/components/log/logged-meals.tsx | Remove and Edit controls measure >=44px hit area at 375px; lines formerly text-neutral-400 (meal-type header, Remove label, macro line) render text-neutral-500; grep "text-neutral-400" in logged-meals.tsx returns 0; smoke 21/21 |
| 6 | FW-06 | /app/dashboard | Loading skeleton mirrors real layout; "Details" link tap target; disable dev-indicator for baselines | medium | src/app/app/dashboard/loading.tsx, src/app/app/dashboard/dashboard-client.tsx, next.config.ts, docs/ui-loop/baseline/app-dashboard--mobile.png | Throttled reload of /app/dashboard shows skeleton with max-w-6xl, dark h-72 hero block, and the lg:grid-cols-[1.15fr_0.85fr] grid (no visible layout snap); "Details" link hit area >=44px via -m-3 p-3; re-captured mobile baseline shows the Home tab icon unobscured (no "N" badge); smoke 21/21 |
| 7 | FW-07 | /app/dashboard, /app/progress, global nav | Raise all sub-12px text to text-xs (12px) | medium | src/app/app/dashboard/dashboard-client.tsx, src/components/layout/mobile-nav.tsx, src/components/progress/macro-stacked-bars.tsx, src/app/app/progress/page.tsx | grep -rn "text-\[10px\]\|text-\[11px\]" across the 4 files returns 0; screenshot at 375px shows hero chips and bottom-nav labels do not wrap; chart axis labels are text-xs text-neutral-500; smoke 21/21 |
| 8 | FW-08 | /app/progress | Segmented-control touch targets, aria-pressed on state toggle, remove contradictory aria-label | medium | src/app/app/progress/page.tsx, src/components/progress/series-toggle.tsx | At 375px the Day 0/1/3+ pills, 7/30-day pills, and series pills measure >=44px tall (min-h-[44px] md:min-h-0); accesslint audit_live on /app/progress shows aria-pressed on the state toggle and zero label-in-name violations (aria-label="Mock weight entry" removed, label "Today's weight" is the accessible name); smoke 21/21 |
| 9 | FW-09 | /app/workouts | Hide stray mobile chevron, add focus-visible ring to "Start this workout" Link, fix neutral-400 labels | medium | src/components/workouts/workouts-view.tsx | Screenshot at 375px shows no orphaned left-aligned arrow row in workout cards (chevron hidden sm:block); tabbing to "Start this workout" shows the same primary-500 focus ring as the adjacent Button; "3 options" and timestamps render text-neutral-500; smoke 21/21 |
| 10 | FW-10 | /app/recipes | Like/dislike and diet-chip touch targets >=44px; neutral-400 labels to neutral-500 | medium | src/components/food/preference-toggle.tsx, src/components/food/diet-filter-chips.tsx, src/app/app/recipes/page.tsx | At 375px PreferenceToggle sm hit area and diet chips measure >=44px in devtools with unchanged visual size; chips do not wrap to a new line pattern at 375px vs baseline; "DIET FILTERS" and "12 recipes" render text-neutral-500; smoke 21/21 |
| 11 | FW-11 | /app/settings | Remove fake Off/On pseudo-toggle, disable dead "Request export" button, fix inert email truncate, date-stamp coach-activity rows | medium | src/components/settings/settings-client.tsx, src/components/settings/coach-activity.tsx | Notifications card shows only the "Coming soon" badge (no Off/On div); "Request export" renders disabled (opacity-50, not clickable) with the explanatory line shown statically and showExportNote state deleted; at 375px a 40-char test email ellipsizes inside the Account card; coach-activity rows older than today include a date (e.g. "Jun 9, 3:42 PM"); smoke 21/21 |
| 12 | FW-12 | /app/recipes | Replace hand-rolled RecipeDetail modal with shadcn/Radix Dialog (focus trap + scroll lock) | medium | src/components/recipes/recipe-detail.tsx, src/components/ui/dialog.tsx | With a recipe open: Tab cycles only inside the dialog, focus moves into it on open, body behind does not scroll, Escape closes (manual keydown effect deleted); rounded-3xl white card styling visually matches baseline screenshot; smoke 21/21 |

## Merge notes

- Contrast (text-neutral-400 on white) appeared on 5 routes; fixed within each route's ticket (FW-01, FW-02, FW-05, FW-09, FW-10) rather than one cross-cutting ticket, because each file is already being touched and worktrees stay conflict-free.
- Touch-target findings appeared on 8 routes; grouped per file/route (FW-02, FW-04, FW-05, FW-06, FW-08, FW-10) plus profile-specific items deferred (see below).
- Dropped/deferred (lowest impact, kept the 12-ticket cap): /app/profile edit-name tap targets and the preview-mode name revert (profile-client.tsx). Both are medium but the route is low-traffic demo surface; queue for next sprint.
- FW-06 includes the dev-indicator baseline fix (devIndicators: false in next.config.ts) since it blocks auditing the nav in the same screenshots.
- No ticket changes navigation structure, brand tokens, or feature scope; FW-12 swaps implementation of an existing modal only.
