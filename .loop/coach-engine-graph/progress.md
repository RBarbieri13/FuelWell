# coach-engine-graph — progress log

## Run 1 — 2026-07-22
- ORIENT: init.sh OK. Frontier: ctx-retrieval, iso-user, audit-log, safety-boundaries, ui-baseline.
- Dispatched 5 parallel implementer subagents (one per frontier node). Verification and commits centralized in orchestrator.
- GRAPH CRITIQUE (start-of-run): considered edge safety-boundaries -> intent-router; rejected — boundary enforcement is a system-prompt property checked at verify-gate, independent of routing. No amendments. DAG re-validated OK.
- iso-user: PASS (verified). Fix: ensureConversation now app-layer scoped by user_id (persistence.ts). Verifier ran 3 adversarial leak attempts (artifact-by-id, foreign goal_plans upsert, foreign-userId knowledge merge) — all blocked. Evidence: evidence/iso-user-report.md. Unverified remainder: RLS audited from SQL not live DB.
- audit-log: PASS (verified, zero code changes). 3 tool-execution branches in turn/route.ts, each audits unconditionally; 17-tool coverage table spot-checked. LESSON: failed Supabase audit inserts are logged-and-swallowed — acceptable now, revisit before GA. Evidence: evidence/audit-log-report.md.
- ctx-retrieval: implemented (recipes + groceries domains added to knowledge.ts; 276 unit tests green) — verifier running.
- safety-boundaries: verifier FAIL — contract missed adult rapid-dehydration water cuts and pregnancy/postpartum deficits. Remediation dispatched to original implementer (2 new bullets + RT-13/14 + assertions). LESSON: red-team the *space between* bullets, not just the bullets; category lists invite gap-seeking.
- ui-baseline: audit complete (0 blockers / 0 majors / 5 minors, 0 console errors; 7 evidence files) — verifier running.
- SCHEDULING: act-nutrition and act-fitness share files (tools/, apply-mutation.ts) → running SEQUENTIALLY to avoid edit races. act-nutrition dispatched; act-fitness queued behind it. Graph-critique note: shared-file nodes should declare a resource edge in future graphs.
- ui-baseline: PASS (verified). 0 blockers / 0 majors / 5 minors, 0 console errors both viewports; verifier cross-checked defects against raw probe JSON and screenshots. Defect list feeds ui-final. Evidence: evidence/ui-baseline-*.
- ctx-retrieval: PASS (verified). Recipes + groceries knowledge domains added; verifier independently rebuilt the 10-domain injection table; 276 unit tests green. ADVISORY (feeds intent-router): wantsNutrition regex misses "cook/dinner/shopping/buy/groceries(plural)" — domains still render (slice(-4) fallback + raw grocery list always in prompt), but retrieval depth is shallow on those utterances. Consider widening the regex in intent-router work.
- safety-boundaries: PASS (round 2). 10-bullet contract (orig 3 + 5 round-1 + water-cut/pregnancy round-2); 14-prompt red-team suite, static-verified (no free provider path exists — live run pending, conversion procedure documented in report). Round-2 verifier attempted 7 further bypass classes, all covered. Advisory (non-blocking): self-directed allergen reintroduction only implicitly covered.
- rich-inline: PASS (verified). Pipeline pre-existed (28 artifact types, 1:1 emitter/renderer); node contribution = theme-token fix + 8 unit tests + 2-viewport stub e2e + screenshots. Caveat: client-side render path proven via stubs; server emission covered by unit tests only (live-model e2e pending, same as safety). DEBT flagged: hardcoded hexes in StreamingTextBubble.tsx and GroceryListCard.tsx (pre-existing, not fixed — token sweep candidate).
- act-nutrition: implemented (fuzzy food/meal/recipe resolution wired into tools via existing rankedSearch; deterministic direct-meal-log parser with real portion scaling; confidence gate prevents wrong-food logging; 119/119 affected tests) — verifier running with independent e2e re-run + adversarial ambiguity probe.
- HYGIENE: act-nutrition's stash/pop collided with chat-process's concurrent client-store.ts edit. stash@{0} preserved (holds older client-store variant; also copied to scratchpad). DO NOT drop stash@{0} until chat-process's client-store.ts is verified intact. act-fitness held until act-nutrition verifies+commits (clean diffs).
- act-nutrition: PASS (verified w/ independent e2e re-run). Fuzzy food/meal/recipe resolution + deterministic meal-log parser (real portion math, confidence-gated, delete can never bypass confirm). 119/119 tests. Residual: model-proposes-confirm half of destructive flow is code-read-verified only (paid call). LESSON: existing infra (rankedSearch) covered the need — the gap was wiring, not new code.
- chat-process: implemented (autoscroll fix, cursor pagination, retry chip + retryLastTurn, stale-chip clearing on error paths, 90s SSE watchdog + readable provider notices; unit 308 green claimed) — verifier running. mobile-overflow spec fails ENVIRONMENTALLY (needs FUELWELL_UI_TEST_EMAIL/PASSWORD, GH-secrets only, hard-fail by design) — verifier adjudicating same-reason-at-baseline. log-confirm-chip.tsx flagged as dead code (only _legacy refs) — candidate for removal in a separate task, NOT this mission.
- LESSON (harness): concurrent nodes sharing one worktree caused two clobber events (chat-process re-applied) + one stash collision (act-nutrition). Future graph missions should use worktree isolation for parallel implementers or declare file-ownership locks in the node spec. Recorded for Loop Please skill update.
- chat-process: PASS (verified). Autoscroll, cursor pagination (user-scoping intact), retryLastTurn (no-duplicate proven by e2e), stale-chip clearing, 90s watchdog + readable degradation notices. mobile-overflow spec: environmental fail, identical at baseline (GH-secrets release gate) — excluded from verify-gate by design. Follow-ups (non-blocking): created_at tiebreak, confirmedTool partial-stream retry edge, log-confirm-chip dead code.
- CORRECTION: rich-inline commit 80a8610 missed the WeightTrendSpark token fix (clobbered at commit time by concurrent edit, re-applied after). Recovered line included in chat-process commit. Worktree-contention lesson reinforced.
- act-fitness: implemented (resolveWorkout vs 376-workout catalog; deterministic workout parser; delete_workout destructive; session start/end by fuzzy name; 329/329) — verifier running with parser-chaining and mislogging traps.
- ORCHESTRATOR RULING: "schedule tomorrow's workout" = data-model gap (no planned-workout collection; needs migration = human-reserved). Node judged on everything else; gap goes to final report as product decision. Also noted: plan_workout/suggest_workout draw from 24-exercise list not the 376 catalog — quality follow-up, non-blocking.
- act-fitness: PASS (verified w/ independent e2e). Fuzzy workout resolution (376-catalog), deterministic duration parser, destructive delete_workout confirm-gated. Verifier adversarial flags remediated centrally by orchestrator: parser now bails to model on past-date phrasings (yesterday/last night/weekday) + test. Sign-drop "-30 min" edge left (cosmetic, logged). Product decisions for human: workout scheduling data model; suggest/plan tools use 24-exercise list not catalog. ARCH NOTE (pre-existing, both meal+workout): confirmedTool trusts client claim, no server-side confirm token.
- ui-final: PASS (verified w/ independent re-measurement — all 5 defects reproduced fixed, 0 blockers, keyboard a11y confirmed). Follow-ups (non-blocking): anchor py-3.5 overlaps hit-boxes if adjacent markdown links ever render (no current surface); disclosure lacks aria-controls. Commit includes the concurrent hex→token sweep (StreamingTextBubble, GroceryListCard, new primary-950/sage tokens) — interleaved in the same files, verified green together.
- intent-router: PASS (verified). INTENT_ROUTING_RULES + NAVIGATION_MAP (all facts code-verified true; all 11 routes exist), regex widened per advisory, 25-utterance suite (4 typo rows + anaphora; 4 live e2e + honest static labels), 333/333. Follow-up (non-blocking): open_page enum covers 6/11 mapped routes — widen if deep-linking matters.
- verify-gate: PASS. Full battery exit 0: verify-coach-engine.mjs (own verdict PASS), lint, production build (797 pages), full vitest 333/333, playwright 4 passed/12 key-gated skips, DAG valid. DoD mapping complete; named uncovered spot: confirmedTool server-side trust (pre-existing, no covering check). MISSION COMPLETE — 12/12 nodes, 1 verifier FAIL total (safety round 1, remediated), 0 deadlocks, ~22 node-attempts of 30.
