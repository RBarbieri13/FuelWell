# design-intuition-graph — progress log

## Run 1 — 2026-07-22
- Setup: merged coach-graph work to main (FF to a053865); Vercel preview deployed (fuelwell-preview, Ready, anonymous 200 — SSO blocker not present on this project); iPhone 17 Pro simulator attached showing preview.
- Package validated: 13 nodes, DAG OK. Frontier: design-ruleset + 5 audits (read-only, parallel, shared dev server :3000).
- 6 root subagents dispatched. Fix wave gated behind fix-shell (shared-surface owner) per coach-graph worktree lesson.
- GRAPH CRITIQUE (start): fitness/workouts split and profile/settings split are audit questions, not presumed nodes; coherence node reconciles cross-group needs; accepted. No amendments.
- design-ruleset: PASS (verified — 4/4 citation spot-checks, 3/3 baseline re-runs exact: hex 427, radii 32, fw-page-inner 38). C1-C5 conventions are law for fix nodes. WATCH: a stray root-level evidence/ dir + PNG appeared — some auditor may be writing to the wrong path; reconcile at collection.
- audit-shell: PASS (verified — F1 3.2px Settings sliver + unscrollable nav reproduced; F3 dual person icons + Profile-less menu enumerated; 17/17 routes classified, no orphans). 12 findings: 4 major (nav scroll/Settings reach, movement-cluster naming chaos, dual person icons, user-menu structure incl. Delete Account one tap from shell + dead floating UserMenu), 5 minor, 3 opportunities.
- audit-insight: PASS (verified — all 5 headline claims reproduced with file:line). 31 findings. TRUTH BUGS confirmed: progress page is hardcoded demo (page.tsx:75-184, switcher user-exposed, zero data fetching); planned workouts summed into Active burn/Net calories (detail-surfaces.tsx:176-184); targets 2140/150 (progress) vs 2250/175 (SAMPLE_TARGETS) both render live. Mobile 30d chart illegible = blocker. These route to fix-insight; number-unification is coherence's top mandate.
- audit-fitness: PASS (verified — W1 mobility/strength mismatch reproduced at source (workout-library.ts:133/812/903) and live; W2 completion dead-end confirmed (live session lacks the detail page's "Review in fitness" CTA); A1 activity log is hardcoded 4-signal timeline blind to real workouts; R2 "iscalculated" typo; R1 decorative toggle byte-identical diff). 23 findings → fix-fitness; activity-data unification also feeds coherence.
- audit-identity: PASS (verified — 27/28 findings reproduced exactly; 1 corrected: Units toggle drives Profile correctly, only the Settings editor ignores it — correction appended to the audit so fix-identity targets the real gap). Confirmed: allergies dual-source contradiction (first-load), onboarding tile clipping at 1280x800, unconditional PREVIEW MODE card on auth, launch-preflight ungated in user shell. LESSON: verifiers catching overstated audit claims is exactly why fix nodes wait for verification.
- fix-shell: implemented (F1-F4 majors + F6/F7/F10/F11; no route/sidebar renames so coach nav-map untouched; dead floating UserMenu deleted; hex 427→426) — verifier running with independent re-measurement. Deferred to page nodes (recorded in fix-shell-report.md): fitness H1 retitle → fix-fitness; dashboard avatar/search → fix-insight; preflight link gating + coach mobile subpage links → coherence (coach pages not owned by any design node); 5-tab reduction → coherence (needs usage judgment).
- fix-shell: PASS (verified — 23 independent checks; Settings reachable at 800/700px incl. collapsed rail; menu auth-aware, Profile first, Delete Account demoted; aliases correct; dead code gone; 333/333 + playwright green). ARIA nit (popover lacks role=menu) → coherence. Fix wave unlocked.
- audit-nutrition: PASS (verified — Plan-this-meal silent-log reproduced live +360kcal; orphan pages confirmed by grep; stale Fill-slot aggregates and unconfirmed Clear-list reproduced; G4 corrected to below-fold-not-invisible, correction appended). 32 findings → fix-nutrition. All 5 audits now verified: 126 findings total.
- fix-identity: PASS (verified — onboarding grids 0 overflow/overlap at 1280x800; Settings units honor toggle w/ round-trip integrity (no double-conversion); allergies contradiction gone; auth preview card SSR-clean + localhost-gated; launch-preflight notFound() outside preview; deep-link anchors land at scroll-mt-24). Interrupted by spend limit mid-verify, resumed cleanly. NOTE for coherence: launch-preflight gating must not break the Fastfile candidate pipeline that reads it; UserMenu is mobile-only by pre-existing design (desktop Settings path = sidebar).
- PROTOCOL DEVIATION (budget): spend limit killed 3 fresh-context verifiers mid-run (twice total this mission). fix-fitness/fix-insight/fix-nutrition verified CENTRALLY by orchestrator instead: full gate battery on combined tree (tsc clean, lint clean, vitest 333/333, playwright 16 passed/12 key-skips incl. renamed smoke+workouts specs) + code spot-checks of highest-risk claims (Planned-split in calculateFitnessTotals:192, mobility-reset category:mobility + tracksWeight gate, demo snapshots gone + 2,250 renders on /app/progress, use-meal-plan store + two-action recipe dialog + grocery confirms). Partial dead-verifier observations were consistent (badges live, dashboard 2,250 via curl). Confidence: high on gates/code, lower on untested interaction edges — design-gate screenshot matrix is the backstop.
- fix-fitness: PASS (central). fix-insight: PASS (central). fix-nutrition: PASS (central).
- coherence: PASS (central). Implemented: preflight-link gating on coach attachments (isPreviewHost), user-menu role=menu/menuitem. Verified: launch-preflight page gating is release-pipeline-safe (Fastfile reads lib/env, never the page). Rejected with reasons: sidebar entries for meal-plan/nutrition (contextual links + aliases suffice), 5-tab reduction (needs usage data), dual sign-out (contexts differ). Residuals documented in coherence-report.md.
- design-gate: PASS (central). pnpm build clean; vitest 333/333 (full); playwright 16 passed/12 key-skips; screenshot matrix 34/34 green (17 routes x 2 viewports, 0 console errors, 0 overflow) archived in evidence/design-gate/. MISSION COMPLETE — 13/13 nodes. 2 spend-limit interruptions survived via resume-from-transcript + central verification fallback (deviation logged above).
- POST-MISSION HOTFIX (user-reported, 2026-07-22): (1) DetailSection disclosure — whole header is now the toggle with a rotating ChevronDown pointing at the content it controls (was: ambiguous up-chevron button between header and content); same rotating-chevron standard applied to the ledger toggles and section Hide/Show. (2) Workout library — search form no longer GET-navigates to /app/workouts (Enter/Apply wiped all filter state); zero-result queries now show a prominent accent-bordered "Found 0 workouts" alert with a Clear-all-filters action. Live-verified: Core+plank=20 rows, Enter stays on page, zero-state renders, clear restores; specs 15/15; tsc+lint clean.
